function collatz(n) {
  let arr = [];
  for(let j = 0; n!=1; j++){
    if (n%2==0){n=n/2; arr[j]=n/2}
  else if (n>0){n=n*3+1; arr[j]=n*3+1}; 
  }
  arr.unshift(n);
  return arr.length
  //your code here
}
